---
title: "TechniqueOverride"
linktitle: "TechniqueOverride"
second_title: "Aspose.CAD for Java"
description: "The technique override."
type: docs
weight: 10
url: /java/com.aspose.cad.fileformats.collada.fileparser.elements/techniqueoverride/
---

**Inheritance:** java.lang.Object, ColladaElement

The technique override. Target specific techniques and passes inside a material rather than having to split the effects techniques and passes into multiple effects.

## Constructors

| Constructor | Description |
| --- | --- |
| [TechniqueOverride()](#TechniqueOverride) |  |

## Methods

| Method | Description |
| --- | --- |
| [getReference()](#getReference) | Gets or sets the reference. The reference attribute is required and specifies the SID of a technique. |
| [setReference(String value)](#setReference-java.lang.String) | Gets or sets the reference. The reference attribute is required and specifies the SID of a technique. |
| [getPass()](#getPass) | Gets or sets the pass. The pass attribute is optional and specifies the SID of one pass to execute. If not specified (or empty), then all of the technique’s passes are used. |
| [setPass(String value)](#setPass-java.lang.String) | Gets or sets the pass. The pass attribute is optional and specifies the SID of one pass to execute. If not specified (or empty), then all of the technique’s passes are used. |

### TechniqueOverride() {#TechniqueOverride}
```java
public TechniqueOverride()
```

### getReference() {#getReference}
```java
public final String getReference()
```

Gets or sets the reference. The reference attribute is required and specifies the SID of a technique.

**Returns:** String

### setReference(String value) {#setReference-java.lang.String}
```java
public final void setReference(String value)
```

Gets or sets the reference. The reference attribute is required and specifies the SID of a technique.

### getPass() {#getPass}
```java
public final String getPass()
```

Gets or sets the pass. The pass attribute is optional and specifies the SID of one pass to execute. If not specified (or empty), then all of the technique’s passes are used.

**Returns:** String

### setPass(String value) {#setPass-java.lang.String}
```java
public final void setPass(String value)
```

Gets or sets the pass. The pass attribute is optional and specifies the SID of one pass to execute. If not specified (or empty), then all of the technique’s passes are used.

