---
title: "DwfMergeOptions"
linktitle: "DwfMergeOptions"
second_title: "Aspose.CAD for Java"
description: "The DWF merge options."
type: docs
weight: 10
url: /java/com.aspose.cad.fileformats.dwf/dwfmergeoptions/
---

The DWF merge options.

## Constructors

| Constructor | Description |
| --- | --- |
| [DwfMergeOptions()](#DwfMergeOptions) |  |

## Methods

| Method | Description |
| --- | --- |
| [getMergeType()](#getMergeType) | Gets or sets the merge type. |
| [setMergeType(int value)](#setMergeType-int) | Gets or sets the merge type. |
| [getSourcePath()](#getSourcePath) | Gets or sets the source path. Full path to source image that will be merge with DWF image. |
| [setSourcePath(String value)](#setSourcePath-java.lang.String) | Gets or sets the source path. Full path to source image that will be merge with DWF image. |
| [getSourcePageNames()](#getSourcePageNames) | Gets or sets the source DWF image page names that will be merge with DWF image. If not set, will be merged all pages. |
| [setSourcePageNames(String[] value)](#setSourcePageNames-java.lang.String:A) | Gets or sets the source DWF image page names that will be merge with DWF image. If not set, will be merged all pages. |
| [getDestinationPageNames()](#getDestinationPageNames) | Gets or sets the page names of destination DWF image to witch will be add raster overlay image. If not set, raster overlay image will be add to all pages. |
| [setDestinationPageNames(String[] value)](#setDestinationPageNames-java.lang.String:A) | Gets or sets the page names of destination DWF image to witch will be add raster overlay image. If not set, raster overlay image will be add to all pages. |

### DwfMergeOptions() {#DwfMergeOptions}
```java
public DwfMergeOptions()
```

### getMergeType() {#getMergeType}
```java
public final int getMergeType()
```

Gets or sets the merge type.

**Returns:** int

### setMergeType(int value) {#setMergeType-int}
```java
public final void setMergeType(int value)
```

Gets or sets the merge type.

### getSourcePath() {#getSourcePath}
```java
public final String getSourcePath()
```

Gets or sets the source path. Full path to source image that will be merge with DWF image.

**Returns:** String

### setSourcePath(String value) {#setSourcePath-java.lang.String}
```java
public final void setSourcePath(String value)
```

Gets or sets the source path. Full path to source image that will be merge with DWF image.

### getSourcePageNames() {#getSourcePageNames}
```java
public final String[] getSourcePageNames()
```

Gets or sets the source DWF image page names that will be merge with DWF image. If not set, will be merged all pages.

**Returns:** String[]

### setSourcePageNames(String[] value) {#setSourcePageNames-java.lang.String:A}
```java
public final void setSourcePageNames(String[] value)
```

Gets or sets the source DWF image page names that will be merge with DWF image. If not set, will be merged all pages.

### getDestinationPageNames() {#getDestinationPageNames}
```java
public final String[] getDestinationPageNames()
```

Gets or sets the page names of destination DWF image to witch will be add raster overlay image. If not set, raster overlay image will be add to all pages.

**Returns:** String[]

### setDestinationPageNames(String[] value) {#setDestinationPageNames-java.lang.String:A}
```java
public final void setDestinationPageNames(String[] value)
```

Gets or sets the page names of destination DWF image to witch will be add raster overlay image. If not set, raster overlay image will be add to all pages.

